/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['dummyjson.com' , 'localhost' , 'localhost:3000'],
  },
  // reactStrictMode: true,
}

module.exports = nextConfig
